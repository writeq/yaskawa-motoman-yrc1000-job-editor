## Summary

<!-- What does this change do, and why? -->

## Test plan

<!-- How did you verify this? npm run lint / npm run build, manual steps, screenshots for UI changes. -->

- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manually exercised the affected flow (describe below)

## Checklist

- [ ] If this touches a file format (.JBI, condition files, headers), round-tripping an unmodified file still produces byte-identical output where applicable
- [ ] README updated if this adds/changes a user-facing feature or the Status/roadmap section
- [ ] No undocumented vendor file-format internals were guessed at (see README "Deliberately out of scope" notes) — real data or clearly-marked placeholders only
